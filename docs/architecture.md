⭐ Основной архитектурный документ

Главы:
1. Обзор архитектуры
2. Архитектурные принципы
3. Внешние интерфейсы системы
4. Структура Solution
5. Структура одного сервиса
6. Зависимости между проектами
7. Saga и обмен сообщениями
8. Observability
9. Infrastructure
10. Testing Strategy



Глава 1. Обзор архитектуры

1.1 Назначение проекта
PizzaSaga — это распределённая микросервисная система, демонстрирующая современные подходы к построению высоконагруженных backend-приложений на платформе .NET.

Основная цель проекта — показать практическое применение архитектурных паттернов, используемых в production-системах:
Clean Architecture (упрощённая);
Tactical DDD;
CQRS;
Saga Orchestration;
Transactional Outbox;
Event-Driven Architecture;
OpenTelemetry;
Distributed Tracing;
Resilience;
Health Checks;
централизованная наблюдаемость (Observability).

Проект демонстрирует решение типичных проблем распределённых систем:
согласованность данных;
гарантированную публикацию сообщений;
идемпотентную обработку повторно доставленных сообщений (паттерн Idempotent Consumer);
компенсацию ошибок;
трассировку запросов;
устойчивость к частичным отказам.


1.2 Архитектурная цель
В распределённой системе невозможно использовать одну общую транзакцию между несколькими сервисами и различными хранилищами данных.

Вместо этого проект строится вокруг следующих принципов:
каждый сервис полностью владеет своими данными;
взаимодействие между сервисами происходит посредством событий;
бизнес-процесс координируется Saga Orchestrator;
доставка сообщений гарантируется Transactional Outbox;
все операции являются наблюдаемыми благодаря OpenTelemetry.

Таким образом достигается eventual consistency без использования распределённых транзакций.


1.3 Основные архитектурные принципы
При разработке проекта используются следующие принципы.

Single Responsibility
Каждый сервис отвечает только за одну бизнес-область.

Например:
Order Service — жизненный цикл заказа;
Stock Service — управление остатками товаров;
Payment Service — резервирование и подтверждение оплаты;
Auth Service — аутентификация и выпуск JWT;
API Gateway — единая точка входа в систему.

Database per Service
Каждый микросервис использует собственное хранилище данных.
Сервисы никогда не обращаются напрямую к базам данных друг друга.

Optimistic Concurrency
Для предотвращения конфликтов при одновременном изменении данных используется оптимистичный контроль конкурентного доступа (Optimistic Concurrency).
При изменении агрегата Order проверяется версия записи. Если с момента её чтения данные были изменены другим процессом, операция завершается ошибкой конкурентного доступа, после чего может быть повторно выполнена с использованием актуального состояния сущности.
Например, во время выполнения Order Saga несколько сообщений могут почти одновременно попытаться изменить состояние одного и того же заказа. Проверка версии записи предотвращает потерю изменений и не допускает выполнения некорректных переходов между состояниями.
Использование Optimistic Concurrency позволяет избежать длительных блокировок базы данных и обеспечивает корректную обработку параллельных изменений в распределённой системе.

Обмен информацией осуществляется исключительно через:
HTTP API (при необходимости);
RabbitMQ;
события интеграции.

Это уменьшает связанность сервисов и позволяет независимо изменять внутреннюю модель данных.

Event-Driven Architecture
Основным способом взаимодействия между сервисами являются события.
После завершения бизнес-операции сервис публикует событие, которое может быть обработано другими сервисами.

Например:
Create Order (HTTP POST /orders)
        │
        ▼
OrderCreated (Domain Event)
        │
        ▼
ReserveInventoryCommand
        │
        ▼
InventoryReservedEvent
        │
        ▼
ReservePaymentCommand
        │
        ▼
PaymentReservedEvent
        │
        ▼
OrderCompleted (Domain Event)
        │
        ▼
OrderCompletedEvent (Integration Event)

Ни один сервис не управляет внутренним состоянием другого сервиса.

Eventual Consistency (согласованность в конечном счете)
Это модель согласованности данных в распределенных системах, при которой изменения не отображаются на всех узлах моментально, но гарантируется, что если новые обновления прекратятся, то спустя некоторое время все копии данных станут одинаковыми.
Система не стремится обеспечить мгновенную согласованность между всеми сервисами.
После выполнения локальной транзакции изменения постепенно распространяются через события.

Это позволяет:
избежать распределённых транзакций;
повысить отказоустойчивость;
обеспечить горизонтальное масштабирование.

Loose Coupling (слабая связанность)
Это архитектурный принцип в программировании, при котором компоненты системы слабо зависят друг от друга. Изменения в одном модуле не требуют доработки остальных, так как они взаимодействуют только через заранее определенные интерфейсы или контракты. Это повышает гибкость и упрощает поддержку кода.
Сервисы знают только о контрактах обмена сообщениями.

Они не зависят:
от внутренних моделей друг друга;
от структуры БД других сервисов;
от конкретной реализации бизнес-логики.

Это значительно упрощает развитие системы.


1.4 Архитектурный стиль
Проект сочетает несколько взаимодополняющих архитектурных подходов.

Подход						Назначение
Clean Architecture			разделение ответственности внутри каждого сервиса
Tactical DDD				моделирование предметной области
CQRS						разделение операций изменения и чтения данных
Vertical Slice				организация Application слоя по бизнес-функциям
Event-Driven Architecture	взаимодействие сервисов
Saga						координация распределённой бизнес-операции
Transactional Outbox		гарантированная публикация сообщений

Каждый паттерн решает собственную задачу:
Clean Architecture определяет структуру сервиса;
CQRS определяет организацию бизнес-операций;
Vertical Slice определяет структуру Application слоя;
Saga определяет взаимодействие между сервисами.


1.5 Технологический стек
Компонент			Технология						Назначение
Платформа			.NET 10							Основная платформа разработки
API					ASP.NET Core Minimal API		Реализация HTTP API
Оркестрация			.NET Aspire AppHost				Запуск всей распределённой системы
API Gateway			YARP							Единая точка входа
Сообщения			RabbitMQ						Асинхронное взаимодействие сервисов
Message Bus			MassTransit						Работа с RabbitMQ и реализация Saga
Saga				MassTransit State Machine		Оркестрация распределённой бизнес-операции
Outbox				MassTransit Transactional Outbox	Гарантированная доставка сообщений
ORM					Entity Framework Core			Работа с PostgreSQL
Основная БД			PostgreSQL						Транзакционные данные
Observability		OpenTelemetry					Трейсинг, метрики, логирование
Dashboard			.NET Aspire Dashboard			Наблюдаемость системы
Resilience			Microsoft.Extensions.Resilience (Polly) Retry, Timeout, Circuit Breaker
Контейнеризация		Docker Compose					Локальный запуск инфраструктуры
Документация API	Swagger / OpenAPI				Документирование HTTP API
API Versioning		ASP.NET API Versioning			Поддержка эволюции HTTP API
Обработка ошибок	ProblemDetails					Стандартизированный формат HTTP-ответов об ошибках согласно RFC 9457

Resilience (отказоустойчивость) — это способность приложения или сервиса правильно реагировать на временные сбои (например, разрывы сети или падения сторонних API), продолжая свою работу без полного отказа.


1.6 Общая архитектура системы
						   +--------------------+
                           |       Client       |
                           +---------+----------+
                                     |
                               HTTPS + JWT
                                     |
                                     ▼
                        +-------------------------+
                        |       API Gateway       |       Аутентификация /
                        |          YARP           +------------+ Проверка JWT
                        +------------+------------+            | (Выдача токенов)
                                     |                         ▼
                                     |              +--------------------+
                                     |              |    Auth Service    |
                                     |              +--------------------+
                                     ▼
                    +----------------------------------+
                    |          Order Service           |
                    |----------------------------------|
                    | REST API                         |
                    | Saga State Machine               |
                    | Transactional Outbox             |
                    +----------------+-----------------+
                                     ▲
                                     |
                          Publish / Consume Events
                                     |
                                     ▼
                      +--------------------------+
                      |         RabbitMQ         |
                      |        MassTransit       |
                      +------------+-------------+
                                   ▲
                                   |
                   +---------------+---------------+
                   |                               |
                   ▼                               ▼
        +--------------------+          +--------------------+
        |    Stock Service   |          |   Payment Service  |
        +--------------------+          +--------------------+

Все сервисы являются независимыми процессами и могут масштабироваться отдельно друг от друга.

1.7 Основной бизнес-процесс
Основным сценарием системы является оформление заказа. Бизнес-процесс представляет собой последовательность локальных транзакций, выполняемых различными микросервисами и координируемых Order Saga.

Типичный сценарий успешного выполнения выглядит следующим образом:

HTTP POST /orders
        │
        ▼
OrderCreated (Domain Event)
        │
        ▼
ReserveInventoryCommand
        │
        ▼
InventoryReservedEvent
        │
        ▼
ReservePaymentCommand
        │
        ▼
PaymentReservedEvent
        │
        ▼
OrderCompleted (Domain Event)
        │
        ▼
OrderCompletedEvent

Если один из этапов завершается неуспешно, Order Saga инициирует компенсирующие действия. Например, при ошибке резервирования оплаты ранее зарезервированный товар освобождается, после чего заказ переводится в состояние Cancelled.

Подробное описание жизненного цикла заказа, состояний Saga, используемых команд, интеграционных событий и компенсационных сценариев приведено в главе 7 «Saga и обмен сообщениями».


1.8 Наблюдаемость системы
Каждый запрос получает уникальный TraceId и CorrelationId.

Эти идентификаторы автоматически передаются:
между HTTP-запросами;
через RabbitMQ;
через MassTransit;
в журналы;
в трассировки OpenTelemetry;
в метрики.

Это позволяет полностью восстановить цепочку выполнения любого бизнес-процесса независимо от количества задействованных сервисов.


1.9 Почему именно такая архитектура
Цель проекта — не продемонстрировать максимальное количество технологий, а показать применение архитектурных решений, характерных для современных распределённых систем.

Выбранный подход обеспечивает:
независимость сервисов;
возможность горизонтального масштабирования;
высокую отказоустойчивость;
наблюдаемость всех компонентов системы;
гарантированную доставку сообщений;
поддержку длительных бизнес-процессов через Saga;
минимальную связанность между сервисами;
удобство дальнейшего развития проекта.

Каждый архитектурный паттерн используется только там, где он решает конкретную задачу. Это позволяет избежать избыточной сложности и сохранить архитектуру эволюционной, когда новые механизмы внедряются только при появлении соответствующих требований.


2. Архитектурные принципы
Данная глава описывает архитектурные подходы, лежащие в основе проекта PizzaSaga.
Архитектура проекта не следует какой-либо одной методологии. Вместо этого используется комбинация нескольких взаимодополняющих подходов, каждый из которых решает определённую инженерную задачу.

Основными архитектурными принципами являются:
Clean Architecture;
Tactical Domain-Driven Design (DDD);
CQRS;
Vertical Slice Architecture;
Event-Driven Architecture.

Помимо перечисленных архитектурных подходов, в проекте применяются инженерные практики, обеспечивающие надёжность распределённой системы: Saga Orchestration, Transactional Outbox, Idempotent Consumers и Optimistic Concurrency. Они рассматриваются в отдельных главах документации, поскольку относятся не к организации кода, а к обеспечению согласованности данных и отказоустойчивости.

Использование нескольких подходов позволяет получить систему, которая остаётся простой для сопровождения, хорошо масштабируется и соответствует современным практикам разработки распределённых приложений на платформе .NET.


2.1 Clean Architecture
Все микросервисы проекта построены в соответствии с принципами Clean Architecture.
Основная цель — отделить бизнес-логику от инфраструктуры и внешних технологий.

В отличие от упрощённой реализации с разделением по папкам, в данном проекте используется физическое разделение слоёв на отдельные проекты.
Каждый слой имеет собственную область ответственности и компилируется в отдельную сборку.
Это позволяет контролировать зависимости между слоями не только архитектурными соглашениями, но и средствами компилятора.

Основной принцип
Все зависимости направлены внутрь приложения.

Api
      │
      ▼
Application
      │
      ▼
Domain
      ▲
      │
Infrastructure

Внутренние слои ничего не знают о внешних.

Domain не зависит от Application слое.
Application также не зависит от инфраструктурных технологий.
Благодаря этому бизнес-логика остаётся независимой от конкретных технологий хранения данных, обмена сообщениями и публикации API.

В PizzaSaga каждый слой представлен отдельным проектом.
Это позволяет:
контролировать зависимости на уровне компиляции;
уменьшить связанность компонентов;
упростить сопровождение;
облегчить модульное тестирование;
сделать архитектуру более явной.

Такой подход ближе к архитектуре, применяемой в крупных корпоративных проектах.


2.2 Tactical Domain-Driven Design
Проект использует тактические паттерны Domain-Driven Design.
Полная реализация стратегического DDD не требуется, поскольку предметная область проекта относительно компактна.
Используются только те элементы DDD, которые действительно повышают выразительность бизнес-модели.

Aggregate
Каждая бизнес-сущность представлена отдельным Aggregate Root.

Например:
Order
Inventory
PaymentReservation

Именно Aggregate управляет собственным состоянием.
Изменение состояния выполняется исключительно через его методы.
Aggregate также отвечает за соблюдение бизнес-инвариантов и обеспечивает консистентность данных внутри своих границ.

Например:
order.Cancel();
order.MarkInventoryReserved();
order.MarkPaymentReserved();

Внешний код не должен изменять состояние агрегата напрямую.
Если бизнес-логика не принадлежит конкретному Aggregate, допускается использование Domain Service.

Entity
Entity обладают собственной идентичностью и изменяемым состоянием. В большинстве случаев они входят в состав Aggregate.

Например:
Order
OrderItem
InventoryItem
PaymentReservation

Value Object
Value Object описывают значения, не имеющие собственной идентичности.

Например:
Money
Address
PhoneNumber
Email

Value Object являются неизменяемыми.
Равенство Value Object определяется их значением, а не ссылкой на объект.
Это уменьшает вероятность ошибок и делает бизнес-модель более предсказуемой.

Domain Events
Domain Events отражают изменения внутри предметной области.

Например:
OrderSubmitted
OrderCancelled
PaymentConfirmed

Domain Events используются только внутри сервиса.
Для взаимодействия между микросервисами используются интеграционные команды и интеграционные события, определённые в общем контракте сообщений.
Это позволяет разделить внутреннюю модель предметной области и внешний контракт взаимодействия.

Что сознательно не используется
В проекте намеренно отсутствуют следующие паттерны:
Generic Repository поверх Entity Framework Core;
Unit of Work поверх DbContext;
Service Locator;
Anemic Domain Model (анемичная модель предметной области) классы-сущности содержат только данные;
Factory используются только тогда, когда создание агрегата невозможно выразить обычным конструктором.

Отказ от этих паттернов обусловлен стремлением сохранить архитектуру максимально простой и выразительной.


2.3 CQRS
Command Query Responsibility Segregation (CQRS) — это архитектурный паттерн, который разделяет операции чтения и записи данных в приложении на две независимые модели.
В проекте используется упрощённая реализация Command Query Responsibility Segregation.

CQRS разделяет операции изменения данных и операции чтения.

Commands
↓
Business Logic
↑
Queries

Такое разделение делает бизнес-логику более понятной и облегчает дальнейшее развитие проекта.

Commands
Команда представляет собой запрос на изменение состояния системы.

Например:
CreateOrder
CancelOrder
ReserveInventory
ReservePayment

Каждая команда имеет единственный обработчик.

Обработчик выполняет:
валидацию;
выполнение бизнес-операции;
сохранение изменений.
После сохранения инфраструктурный слой публикует накопленные Domain Events.

Queries
Запрос не изменяет состояние системы.

Например:
GetOrder
GetOrderStatus
GetInventory

Запросы читают данные напрямую из базы данных.
При необходимости в будущем может быть выделена отдельная модель чтения.

В качестве реализации CQRS используется библиотека Mediator. Она обеспечивает статическую генерацию обработчиков через Source Generators, поддерживает Pipeline Behaviors и не требует регистрации обработчиков через Reflection, что упрощает навигацию по коду и делает решение совместимым с Native AOT.

Pipeline Behaviors
Повторяющаяся функциональность выносится в Pipeline Behaviors.

Например:
ValidationBehavior
LoggingBehavior
TransactionBehavior
PerformanceBehavior

Это позволяет оставить обработчики максимально компактными и сосредоточенными исключительно на бизнес-логике.


2.4 Vertical Slice Architecture
Внутри слоя Application используется подход Vertical Slice Architecture.
Архитектура вертикального среза Vertical Slice Architecture (VSA) — это подход к проектированию программных систем, при котором приложение делится на независимые функции (фичи), а не на технические уровни (слои).

В отличие от классического разделения по техническим слоям, код организуется вокруг бизнес-функций.

Например:

Application
└── Features
    └── Orders
        ├── Create
        ├── Cancel
        └── Get

Каждая бизнес-функция содержит все необходимые компоненты.

Например:
Create
CreateOrderCommand
CreateOrderHandler
CreateOrderValidator
CreateOrderRequest
CreateOrderResponse

Благодаря этому изменения оказываются локализованы внутри одной функциональной области и упрощает дальнейшее развитие системы.

Почему Vertical Slice используется только в Application?
Vertical Slice описывает прикладные сценарии использования системы.

Domain моделирует предметную область и не должен зависеть от конкретных пользовательских сценариев.

Поэтому Domain организуется по бизнес-сущностям:
Aggregates
Entities
ValueObjects
Domain Events

а Application — по сценариям использования.

Такое разделение позволяет совместить выразительную доменную модель с удобной организацией прикладного кода.


2.5 Event-Driven Architecture
Event-Driven Architecture (EDA или событийно-ориентированная архитектура) — это подход к проектированию ПО, при котором компоненты системы общаются не через прямые синхронные запросы (например, REST API), а через публикацию и обработку событий — сигналов об изменении состояния.
Межсервисное взаимодействие строится на сочетании синхронных HTTP-запросов (через API Gateway) и асинхронного обмена интеграционными событиями. Длительные бизнес-процессы реализуются посредством событийного взаимодействия.
Каждый сервис публикует события о произошедших изменениях и реагирует на события других сервисов.

Такой подход обеспечивает:
слабую связанность сервисов;
независимое масштабирование;
отказоустойчивость;
поддержку длительных бизнес-процессов.

Для доставки сообщений используется RabbitMQ, а для управления бизнес-процессом — Saga Orchestrator на базе MassTransit.


2.6 Взаимодействие архитектурных подходов
Используемые архитектурные подходы дополняют друг друга.

Подход						Назначение
Clean Architecture			Разделение системы на независимые слои
Tactical DDD				Моделирование предметной области
CQRS						Разделение операций чтения и изменения данных
Vertical Slice				Организация прикладного кода по бизнес-функциям
Event-Driven Architecture	Взаимодействие микросервисов посредством событий

Каждый подход решает собственную задачу и применяется только там, где это действительно необходимо.


2.7 Основные архитектурные принципы

При разработке всех сервисов проекта соблюдаются следующие правила:
бизнес-логика не зависит от инфраструктуры;
каждый Aggregate самостоятельно управляет своим состоянием;
команды и запросы имеют независимые обработчики;
каждая бизнес-операция реализуется отдельным обработчиком;
повторяющаяся функциональность выносится в Pipeline Behaviors;
взаимодействие между сервисами осуществляется только через публичные API или обмен сообщениями;
внутренние модели сервиса никогда не используются другими сервисами;
предпочтение отдаётся простым решениям, если они удовлетворяют требованиям системы;
интеграционные события публикуются только после успешного сохранения данных с использованием Transactional Outbox;
обработчики сообщений проектируются идемпотентными, чтобы повторная доставка сообщений не приводила к повторному выполнению бизнес-операций.


3. Внешние интерфейсы системы
3.1. Назначение
Внешние интерфейсы определяют способы взаимодействия клиентов с системой независимо от внутренней реализации микросервисов.

Основным способом взаимодействия является HTTP REST API через API Gateway. В дальнейшем архитектура допускает использование дополнительных механизмов интеграции (WebSocket, Webhook, gRPC и др.) без изменения внутренних бизнес-процессов.
Для внешнего клиента система представляет собой единый API независимо от количества микросервисов, участвующих в обработке запроса.


3.2. Общая модель взаимодействия
Все клиентские запросы поступают через API Gateway.
Публичный API поддерживает версионирование. Все клиентские запросы выполняются через соответствующую версию API (например, /api/v1/orders), что позволяет развивать контракт без нарушения обратной совместимости.

Client
    │
    ▼
API Gateway
    │
    ▼
Микросервис
    │
    ▼
Business Logic

API Gateway отвечает за:
маршрутизацию запросов;
проверку JWT-токенов и передачу информации о пользователе во внутренние сервисы;
централизованную обработку версий API;
обеспечение обратной совместимости при развитии публичного API;
унифицированное представление ошибок (ProblemDetails);
распространение CorrelationId между сервисами;
сокрытие внутренней структуры микросервисов;
единый вход в систему.

Каждый микросервис предоставляет собственную OpenAPI-спецификацию (Swagger) для документирования своих REST API. API Gateway не агрегирует документацию сервисов, поскольку OpenAPI предназначена главным образом для разработки, тестирования и сопровождения отдельных сервисов.

Клиент не взаимодействует с микросервисами напрямую.


3.2.1 Контракты API

Все публичные REST API используют единые соглашения:
JSON в качестве основного формата обмена;
HTTP-коды состояния в соответствии со спецификацией REST;
ProblemDetails для представления ошибок;
единые правила именования ресурсов;
версионирование API.

Это обеспечивает единообразие взаимодействия клиентов со всеми сервисами системы.


3.3 Аутентификация
Перед обращением к защищённым методам клиент должен пройти аутентификацию и получить JWT-токен.
Для этого используется Auth Service.

Общий сценарий взаимодействия выглядит следующим образом.

POST /auth/login
        │
        ▼
Проверка пользователя
        │
        ▼
JWT Access Token
        │
        ▼
Client
        │
        ▼
Authorization: Bearer <token>
        │
        ▼
API Gateway

После успешной аутентификации клиент использует полученный JWT-токен при обращении ко всем защищённым REST API.
JWT-токен передаётся в заголовке Authorization по схеме Bearer. 
Проверка JWT выполняется централизованно в API Gateway. Внутренние сервисы не взаимодействуют с Auth Service напрямую при обработке каждого запроса.


3.4. Синхронные и асинхронные операции
В системе используются два типа операций.

Синхронные операции
Запрос завершается в рамках одного HTTP-вызова.

Примеры:
получение списка заказов;
получение информации о товаре;
изменение профиля пользователя;
получение статуса заказа.

Такие операции обычно возвращают код ответа:
200 OK;
201 Created;
204 No Content.

Асинхронные операции
Некоторые бизнес-процессы требуют взаимодействия нескольких микросервисов и не могут быть завершены за время одного HTTP-запроса.

Например:
оформление заказа;
возврат денежных средств;
создание доставки;
выполнение сложных бизнес-процессов.

В подобных случаях сервис:
принимает запрос;
сохраняет первоначальное состояние;
инициирует выполнение бизнес-процесса;
сразу возвращает ответ клиенту.

Клиент получает подтверждение о принятии запроса, а не о завершении бизнес-операции.
Сам процесс продолжается асинхронно.


3.5. Асинхронная модель обработки
Для длительных бизнес-процессов HTTP-запрос не ожидает завершения всех внутренних операций.
После получения запроса сервис создаёт новый ресурс и возвращает клиенту ответ 201 Created с его идентификатором и начальным состоянием Pending.
Общая схема обработки выглядит следующим образом.

POST /orders
        │
        ▼
201 Created
Status = Pending
        │
        ▼
GET /orders/{id}
        │
        ▼
Completed
POST /orders

Ответ:
201 Created
{
    "orderId": "8e24b5d2",
    "status": "Pending"
}

Возвращение ответа 201 Created означает успешное создание ресурса, но не завершение полного бизнес-процесса.
После возврата ответа дальнейшая обработка продолжается асинхронно до достижения конечного состояния заказа.

Например:
резервирование товара;
проведение оплаты;
создание доставки;
отправка уведомлений.

Итоговый результат будет сформирован позже после завершения всех этапов обработки.


3.6. Получение состояния бизнес-процесса
После создания ресурса клиент может получить его текущее состояние.

Например:
GET /orders/{id}

Ответ во время выполнения процесса:
{
    "orderId": "8e24b5d2",
    "status": "Pending"
}

После успешного завершения:
{
    "orderId": "8e24b5d2",
    "status": "Completed"
}

При невозможности завершить процесс:
{
    "orderId": "8e24b5d2",
    "status": "Cancelled"
}

Конкретный набор состояний определяется бизнес-логикой соответствующего сервиса. При этом клиент взаимодействует только с текущим состоянием ресурса, не завися от внутреннего механизма выполнения бизнес-процесса.


3.7. Способы уведомления клиента
Архитектура поддерживает несколько моделей получения результата выполнения асинхронных операций.

Polling (опрос состояния ресурса)
Наиболее распространённый вариант.
Именно этот способ используется в рамках данного проекта.
После получения идентификатора ресурса клиент периодически выполняет запрос:
GET /orders/{id}
до тех пор, пока процесс не перейдёт в конечное состояние.

Преимущества:
простая реализация;
отсутствие постоянного соединения;
подходит большинству клиентов.

Архитектура также допускает использование push-механизмов (например, WebSocket, SignalR или Server-Sent Events), а также Webhook при интеграции с внешними системами. В рамках текущего проекта данные способы уведомления не реализуются, однако публичный API не ограничивает их последующее добавление.


3.8. Независимость внешнего API от внутренней реализации
Контракт внешнего API не зависит от механизма реализации бизнес-процесса.

Внутри системы могут использоваться различные механизмы координации и обмена сообщениями, такие как Saga, Transactional Outbox, брокеры сообщений и компенсационные операции.

Для клиента все эти детали остаются скрытыми. Он взаимодействует только с публичным API и состоянием ресурсов.
Внутренние механизмы обмена сообщениями и координации между микросервисами рассматриваются в главе 7. Saga и обмен сообщениями.


4. Структура Solution
4.1 Общая организация решения

Репозиторий организован по принципу Service First (ориентация на сервисы). Каждый микросервис имеет собственную структуру, основанную на подходе Feature First (ориентация на функционал) и принципах Clean Architecture.

На верхнем уровне репозитория находятся только крупные логические области:
инфраструктура запуска;
общие библиотеки;
бизнес-сервисы.

Такая структура хорошо масштабируется при увеличении количества микросервисов и позволяет быстро ориентироваться в репозитории.

Полная структура решения выглядит следующим образом.

PizzaSaga.slnx
│
├── docs
│
├── src
│   │
│   ├── Host
│   │   ├── PizzaSaga.AppHost
│   │   └── PizzaSaga.ServiceDefaults
│   │
│   ├── Gateway
│   │   └── PizzaSaga.ApiGateway
│   │
│   ├── Shared
│   │   ├── PizzaSaga.Contracts
│   │   └── PizzaSaga.BuildingBlocks
│   │
│   └── Services
│       │
│       ├── Order
│       ├── Stock
│       ├── Payment
│       └── Auth
│
└── tests

Такое разделение позволяет легко расширять систему без изменения существующей структуры репозитория.
При добавлении нового микросервиса достаточно создать новую папку внутри каталога Services.


4.2 Назначение каталогов верхнего уровня
docs
Каталог содержит архитектурную документацию проекта.

Например:
architecture.md
api.md
adr/

Документация развивается вместе с кодовой базой и рассматривается как полноценная часть проекта.

src
Каталог содержит весь исходный код приложения.
Внутри него располагаются только проекты, участвующие в работе системы.

tests
Содержит System Tests. Тесты описаны в главе 10 Testing Strategy.


4.3 Host
Каталог Host содержит проекты, отвечающие за запуск и конфигурацию распределённой системы.

Host
├── PizzaSaga.AppHost
└── PizzaSaga.ServiceDefaults

Эти проекты не содержат бизнес-логики.
Их задача — обеспечить единообразную инфраструктуру для всех сервисов.

PizzaSaga.AppHost
AppHost является точкой входа для запуска всей распределённой системы.

Он отвечает за:
запуск всех сервисов;
запуск контейнеров;
композицию приложения и регистрацию сервисов DI;
Service Discovery;
передачу конфигурации;
открытие Aspire Dashboard.

AppHost не содержит бизнес-логики и не участвует в обработке пользовательских запросов.

PizzaSaga.ServiceDefaults
Проект содержит повторно используемую инфраструктурную конфигурацию.

Например:
OpenTelemetry;
Health Checks;
стандартные политики Resilience;
Service Discovery;
HTTP Client;
стандартные политики Service Discovery;
логирование.

Все сервисы используют одинаковые настройки через данный проект.
Это позволяет централизованно изменять инфраструктурное поведение всей системы.


4.4 Gateway
Каталог содержит единственную внешнюю точку входа в систему.

Gateway
└── PizzaSaga.ApiGateway

API Gateway построен на основе YARP Reverse Proxy.
Gateway не агрегирует данные из нескольких сервисов и не содержит бизнес-логики.

Он отвечает за:
маршрутизацию запросов;
проверку JWT;
авторизацию;
Rate Limiting;
передачу CorrelationId;
Health Endpoints.

Внутри Gateway отсутствует бизнес-логика.


4.5 Shared
Каталог содержит общие библиотеки, используемые несколькими сервисами.

Shared
├── PizzaSaga.Contracts
└── PizzaSaga.BuildingBlocks

Любое изменение общего компонента должно оцениваться с точки зрения влияния на все зависимые сервисы.
Общие библиотеки должны содержать только действительно универсальные компоненты.

PizzaSaga.Contracts
Проект содержит исключительно контракты взаимодействия между сервисами.

Допускается размещение:
Commands;
Integration Events;
DTO, используемых в межсервисном взаимодействии;
общих перечислений (при необходимости).

Например:
Commands
ReserveInventory
ReservePayment
Events
InventoryReserved
PaymentReserved
OrderCompleted

Запрещается размещать:
Entity Framework Core;
бизнес-логику;
Aggregate;
Entity;
репозитории;
сервисы;
инфраструктурный код.

Контракты являются публичным API взаимодействия между микросервисами.

PizzaSaga.BuildingBlocks
Содержит общие инфраструктурные компоненты.

Например:
базовые классы Domain Events;
Result<T>;
Guard Clauses;
Middleware;
общие расширения;
вспомогательные классы.

BuildingBlocks не содержит бизнес-логики.
Основная задача проекта — уменьшение дублирования инфраструктурного кода.
При добавлении нового компонента необходимо убедиться, что он действительно является общим, а не относится к одному конкретному сервису.


4.6 Services
Каталог содержит все бизнес-сервисы системы.

Services
├── Order
├── Stock
├── Payment
└── Auth

Каждый сервис представляет собой полностью самостоятельное приложение.

Каждый сервис имеет:
собственную модель предметной области;
собственную базу данных;
собственные API;
собственную инфраструктуру;
собственную очередь сообщений;
независимый жизненный цикл.

Такой подход соответствует принципу Database per Service - ни один сервис не обращается напрямую к базе данных другого сервиса. Это позволяет независимо развивать отдельные компоненты системы.

Order
Отвечает за жизненный цикл заказа.

Основные обязанности:
создание заказа;
управление состояниями заказа;
запуск Saga;
публикация событий;
координация распределённого бизнес-процесса.

Stock
Отвечает за управление складскими остатками.

Основные обязанности:
управление товарами;
резервирование товара;
освобождение резервов;
публикация событий о состоянии склада.

Payment
Отвечает за обработку платёжных операций.

Основные обязанности:
резервирование денежных средств;
подтверждение оплаты;
отмена резервирования;
публикация событий оплаты.

Auth
Предоставляет аутентификацию пользователей.

Основные обязанности:
выпуск JWT;

В текущей версии проекта используется упрощённая реализация, которую в дальнейшем можно заменить полноценным Identity Provider.

4.6.1 Автономность сервисов
Каждый сервис разрабатывается и сопровождается как самостоятельное приложение. Межсервисное взаимодействие осуществляется исключительно через публичные API и обмен сообщениями. Использование внутренних компонентов другого сервиса или прямой доступ к его базе данных не допускаются.


4.7 Принципы организации Solution
При организации репозитория используются следующие правила.

Принцип единственной ответственности
Каждый проект отвечает только за одну область ответственности.

Например:
AppHost — только запуск системы.
Contracts — только контракты.
ApiGateway — только маршрутизация.
Order — только управление заказами.

Принцип независимости сервисов

Каждый сервис должен иметь возможность:
разрабатываться независимо;
тестироваться независимо;
развёртываться независимо;
масштабироваться независимо.

Принцип минимальных зависимостей
Проекты должны зависеть только от компонентов, необходимых для выполнения их обязанностей.
Следует избегать избыточных ссылок между сервисами и общими библиотеками.

Принцип явных границ
Все архитектурные границы отражаются физической структурой репозитория.
Это позволяет быстрее понимать устройство системы даже разработчикам, впервые открывшим проект.

Принцип масштабируемости
Добавление нового сервиса не требует изменения существующей структуры Solution.

Например, новый сервис доставки может быть добавлен следующим образом:
Services
├── Order
├── Stock
├── Payment
├── Auth
└── Delivery

При этом остальные проекты останутся неизменными.


4.8 Итог
Структура Solution отражает архитектуру всей системы и разделяет проекты по их назначению:

Host — запуск и инфраструктурная композиция приложения.
Gateway — единая точка входа.
Shared — общие библиотеки и контракты.
Services — независимые бизнес-сервисы.
Tests — автоматическое тестирование.
Docs — архитектурная документация.

Такое разделение делает репозиторий понятным, предсказуемым и готовым к дальнейшему развитию без изменения базовой структуры.


5. Структура одного сервиса
5.1 Общая организация
Каждый бизнес-сервис в системе построен по единым архитектурным правилам.

Независимо от предметной области (Order, Stock, Payment или Auth), сервис имеет одинаковую внутреннюю структуру и состоит из нескольких проектов, соответствующих слоям Clean Architecture.

Такой подход обеспечивает:
единообразие структуры всех сервисов;
независимое развитие компонентов;
явные архитектурные границы;
удобство сопровождения и тестирования.

В качестве примера далее рассматривается структура Order Service.


5.2 Структура сервиса
Services
└── Order
    │
    ├── Order.Api
    ├── Order.Application
    ├── Order.Domain
    ├── Order.Infrastructure
    │
    ├── Order.ArchitectureTests
    ├── Order.UnitTests
    └── Order.IntegrationTests

Каждый проект имеет собственную область ответственности.


5.3 Order.Api
Проект Order.Api является точкой входа в сервис.
Он отвечает исключительно за взаимодействие с внешним миром.

Основные обязанности
Minimal API Endpoints;
Swagger / OpenAPI;
Dependency Injection;
обработка HTTP-запросов;
преобразование HTTP ↔ DTO;
конфигурация приложения;
публикация Health Checks.

Что НЕ должно находиться в Api
Проект не должен содержать:
бизнес-логику;
работу с Entity Framework Core;
обращения к RabbitMQ;
доменные модели;
реализацию сценариев использования.

Endpoint должен выполнять единственную задачу — принять запрос и передать его в слой Application.

Пример структуры
Order.Api

Endpoints/
Middleware/
Configuration/
Extensions/
Program.cs


5.4 Order.Application
Application представляет прикладной слой системы.
Именно здесь реализуются сценарии использования (Use Cases).
Каждый пользовательский сценарий оформляется как отдельная функциональная область (Vertical Slice).

Основные обязанности:
Commands;
Queries;
Handlers;
Validators;
Pipeline Behaviors;
интерфейсы инфраструктуры;
DTO;
Application Services (при необходимости).

Пример структуры 
Order.Application

Features
│
├── CreateOrder
│   ├── CreateOrderCommand
│   ├── CreateOrderHandler
│   ├── CreateOrderValidator
│   ├── CreateOrderRequest
│   └── CreateOrderResponse
│
├── CancelOrder
│
└── GetOrder

Каждая функциональная область полностью инкапсулирует реализацию одного сценария.
Это позволяет изменять бизнес-функциональность локально, не затрагивая остальные части приложения.

Интерфейсы
Application определяет абстракции, необходимые для своей работы:
IOrderRepository
IClock
IUserContext

Конкретные реализации находятся в Infrastructure.
Application ничего не знает о способе хранения данных или используемом брокере сообщений.

Pipeline Behaviors
Повторяющаяся логика реализуется через Pipeline Behaviors:
ValidationBehavior
LoggingBehavior
TransactionBehavior
PerformanceBehavior

Это позволяет оставить обработчики максимально компактными.

Координация выполнения сценариев
Слой Application отвечает за координацию выполнения бизнес-сценариев. При необходимости здесь реализуются проверки идемпотентности операций, управление транзакциями и обработка ошибок конкурентного доступа к данным (Optimistic Concurrency).


5.5 Order.Domain
Domain является центральной частью приложения.
Именно здесь располагается бизнес-модель.
Domain не зависит ни от одного внешнего компонента.

Основные обязанности
Aggregates;
Entities;
Value Objects;
Domain Events доменные события, отражающие изменения состояния агрегатов;
бизнес-правила;
доменные сервисы (при необходимости);
спецификации (при необходимости).

Пример структуры
Order.Domain

Aggregates/
Entities/
ValueObjects/
DomainEvents/
Exceptions/
Services/

Основные требования Domain:
не использует Entity Framework Core;
не использует ASP.NET Core;
не использует MassTransit;
не использует RabbitMQ;
не использует IConfiguration.

Domain представляет собой чистую бизнес-модель.


5.6 Order.Infrastructure
Infrastructure содержит реализацию всех технических зависимостей.
Именно здесь происходит интеграция приложения с внешними системами.

Основные обязанности
Entity Framework Core;
PostgreSQL;
RabbitMQ;
MassTransit;
Outbox;
Consumers;
миграции;
репозитории;
реализация интерфейсов Application.

Пример структуры
Order.Infrastructure
│
├── DependencyInjection
│
├── Persistence
│   │
│   ├── DbContext
│   │   ├── OrderDbContext.cs
│   │   ├── DesignTimeDbContextFactory.cs
│   │   └── ...
│   │
│   ├── Configurations
│   │   ├── OrderConfiguration.cs
│   │   └── ...
│   │
│   ├── Repositories
│   │   ├── OrderRepository.cs
│   │   └── ...
│   │
│   ├── Migrations
│   │
│   └── Interceptors
│
├── Messaging
│   │
│   ├── Consumers
│   ├── Sagas
│   ├── Outbox
│   └── Bus
│
├── Authentication
│
└── Extensions

Infrastructure также содержит конфигурацию политик повторных попыток обработки сообщений (Retry Policies), маршрутизации сообщений в Dead Letter Queue и настройки идемпотентных обработчиков сообщений.

Такая организация разделяет код по областям ответственности (Persistence, Messaging), а уже внутри каждой области — по конкретным компонентам. 
Такая структура хорошо масштабируется.

Репозитории
В проекте не используется Generic Repository.
Однако допускается реализация специализированных репозиториев, необходимых приложению.

Например:
OrderRepository
OrderReadRepository

Они реализуют интерфейсы, объявленные в Application.


5.7 Организация тестирования сервиса
Каждый сервис сопровождается собственным набором тестовых проектов.

Unit Tests
Проверяют бизнес-логику отдельных компонентов.

Integration Tests
Проверяют взаимодействие сервиса с инфраструктурой.

Architecture Tests
Контролируют соблюдение архитектурных ограничений.

Подробная стратегия тестирования, перечень сценариев и используемые инструменты приведены в главе 10 «Testing Strategy».


5.8 Вертикальная организация Application
Несмотря на физическое разделение на проекты, внутри слоя Application используется Vertical Slice Architecture.
Каждая функциональная возможность располагается в собственной директории.

Например:
Order.Application
Features
│
├── CreateOrder
├── CancelOrder
├── ConfirmOrder
├── GetOrder
└── GetOrderStatus

Такой подход имеет ряд преимуществ:
высокая локализация изменений;
минимальное количество зависимостей между сценариями;
простая навигация по коду;
отсутствие крупных "сервисных" классов с десятками методов.


5.9 Жизненный цикл запроса
Ниже показан путь выполнения типичного HTTP-запроса.

HTTP Request
      │
      ▼
Minimal API Endpoint
      │
      ▼
Mediator
      │
      ▼
Pipeline Behaviors
      │
      ▼
Command Handler
      │
      ▼
Aggregate (Domain)
      │
      ▼
Repository (Infrastructure)
      │
      ▼
PostgreSQL
      │
      ▼
Outbox
      │
      ▼
RabbitMQ

Каждый слой отвечает только за свою часть обработки.
Это делает поток выполнения понятным и предсказуемым.


5.10 Принципы организации сервиса
При разработке всех сервисов соблюдаются следующие правила.

Один проект — одна ответственность
Каждый проект выполняет строго определённую функцию.

Domain независим
Domain не знает о существовании остальных слоёв.

Infrastructure реализует интерфейсы
Infrastructure предоставляет реализации интерфейсов, объявленных в Application.

Application управляет бизнес-сценариями
Application координирует выполнение сценариев использования, но не содержит технических деталей работы с внешними системами.

Api остаётся максимально тонким
Endpoint не содержит бизнес-логики.
Его задача — преобразовать HTTP-запрос в команду или запрос и передать управление в Application.


6. Зависимости между проектами
6.1 Назначение
Одним из ключевых принципов проекта является контроль зависимостей между проектами.
Архитектура считается устойчивой только тогда, когда её правила соблюдаются не по договорённости команды, а обеспечиваются самой структурой решения.
Поэтому в PizzaSaga каждый слой реализован в виде отдельного проекта, а зависимости между ними строго ограничены.


6.2 Общая схема зависимостей
Ниже приведена общая схема зависимостей одного сервиса.

					 +-------------------+
					 |     Order.Api     |
					 +---------+---------+
							   │
			  ┌────────────────┴────────────────┐
			  ▼                                 ▼
+---------------------------+     +---------------------------+
|   Order.Application       |<────|   Order.Infrastructure    |
+-------------+-------------+     +-------------+-------------+
              │                                 │
              └─────────────────┬───────────────┘
                                ▼
                     +----------------------+
                     |    Order.Domain      |
                     +----------------------+

Здесь сразу видно:
Api → Application
Api → Infrastructure
Infrastructure → Application
Infrastructure → Domain
Application → Domain

Проект			Зависит от
Api				Application, Infrastructure
Application		Domain
Infrastructure	Application, Domain
Domain			Нет зависимостей

Физические зависимости между проектами соответствуют принципам Clean Architecture.


6.3 Правило зависимостей
Главное правило архитектуры формулируется следующим образом: зависимости между проектами всегда направлены от внешних слоёв к внутренним.
Ни один внутренний слой не должен зависеть от реализации внешнего слоя.

Это означает:
внешний слой может зависеть от внутреннего;
внутренний слой никогда не зависит от внешнего.

Таким образом бизнес-логика остаётся полностью независимой от инфраструктурных технологий.


6.4 Order.Domain
Domain является самым внутренним слоем приложения.
Он не имеет зависимостей ни от одного проекта решения.

Order.Domain
↓
нет зависимостей

Domain содержит исключительно предметную область.

Допустимые зависимости:
BCL (.NET Base Class Library);
небольшие инфраструктурно-независимые библиотеки (например, FluentResults, если они используются во всём проекте).

Недопустимы зависимости от:
ASP.NET Core;
Entity Framework Core;
MassTransit;
RabbitMQ;
PostgreSQL;
IConfiguration;
ILogger;
HttpContext;
OpenTelemetry.

Domain должен оставаться полностью изолированным от инфраструктуры.


6.5 Order.Application
Application реализует сценарии использования системы.

Он зависит только от Domain.

Order.Application
↓
Order.Domain

Application не знает:
где находятся данные;
каким образом отправляются сообщения;
какая используется база данных;
какой брокер сообщений используется.

Именно Application определяет необходимые абстракции (репозитории, внешние сервисы и другие порты), а Infrastructure предоставляет их реализации.


6.6 Order.Infrastructure
Infrastructure реализует все технические зависимости приложения.
Он зависит от Application и Domain.

Order.Infrastructure
↓
Order.Application
↓
Order.Domain

Infrastructure содержит реализации интерфейсов, объявленных в Application.
Это соответствует принципу Dependency Inversion Principle (DIP):
высокоуровневые модули определяют абстракции;
низкоуровневые модули реализуют эти абстракции.
SOLID (буква «D» в аббревиатуре ) его главная цель — сделать модули программы слабосвязанными, чтобы изменения в низкоуровневом коде не ломали высокоуровневый код.

Infrastructure не содержит бизнес-правил.
Его задача — предоставить техническую реализацию прикладному слою.


6.7 Order.Api
Api является внешним слоем приложения.
Он зависит от Application и Infrastructure.

Order.Api
↓
Order.Application
↓
Order.Infrastructure

Основная задача Api — композиция приложения: регистрация зависимостей, настройка middleware, конфигурация приложения и публикация HTTP API.


6.8 Общие проекты
Помимо проектов, относящихся к конкретному сервису, решение содержит две общие библиотеки:
PizzaSaga.Contracts;
PizzaSaga.BuildingBlocks.

Эти проекты предназначены для повторного использования, однако имеют разные области ответственности.

PizzaSaga.Contracts
PizzaSaga.Contracts содержит публичные контракты взаимодействия между микросервисами.

В проекте размещаются только те типы, которые используются при обмене сообщениями или вызовах между сервисами:

Commands
├── ReserveInventoryCommand
├── ReservePaymentCommand
└── CancelOrderCommand

Events
├── InventoryReservedEvent
├── InventoryReservationFailedEvent
├── PaymentReservedEvent
├── PaymentReservationFailedEvent
└── OrderCompletedEvent

Допускается также размещение общих DTO и перечислений, если они являются частью публичного контракта.

Контракты являются публичным API взаимодействия между сервисами и должны оставаться максимально стабильными.
Изменение существующих контрактов должно выполняться с сохранением обратной совместимости. При необходимости развития взаимодействия предпочтительно добавлять новые версии сообщений вместо изменения существующих.

Зависимости от PizzaSaga.Contracts
На проект PizzaSaga.Contracts могут ссылаться любые сервисы системы.
Например:

                      PizzaSaga.Contracts
                      ▲        ▲        ▲        ▲
                      │        │        │        │
                 Order      Stock    Payment    Auth

При этом сервисы не имеют прямых ссылок друг на друга.

PizzaSaga.BuildingBlocks
PizzaSaga.BuildingBlocks содержит общие инфраструктурные компоненты, которые используются несколькими сервисами.

Например:
Result<T>;
Error;
базовый класс DomainEvent;
Guard Clauses;
общие расширения;
вспомогательные классы.

В отличие от PizzaSaga.Contracts, данный проект не является частью публичного API системы.
Его задача — уменьшить дублирование технического кода.
BuildingBlocks должен содержать только универсальные компоненты, пригодные для повторного использования.

Принцип использования общих библиотек
При разработке проекта применяется следующее правило:
Если компонент относится к предметной области конкретного сервиса, он остаётся внутри этого сервиса. Общие библиотеки используются только для действительно универсальных компонентов.

Это позволяет избежать появления скрытых зависимостей между сервисами и сохранить независимость их развития.

Итог
Общие проекты выполняют разные задачи:
Проект						Назначение
PizzaSaga.Contracts			Публичные контракты взаимодействия между микросервисами (команды, интеграционные события, общие DTO)
PizzaSaga.BuildingBlocks	Общие инфраструктурные компоненты и вспомогательные классы

Такое разделение позволяет сохранить чёткие архитектурные границы и предотвращает превращение общих библиотек в место хранения разнородного кода.


6.9 Допустимые зависимости
Ниже приведена матрица допустимых зависимостей.

Проект			Domain	Application	Infrastructure	Api	Contracts	BuildingBlocks
Domain			—		❌			❌				❌	✅			✅
Application		✅		—			❌				❌	✅			✅
Infrastructure	✅		✅			—				❌	✅			✅
Api				❌		✅			✅				—	✅			✅

Примечание: Domain должен зависеть от PizzaSaga.Contracts только в исключительных случаях. Предпочтительно, чтобы доменная модель была полностью независимой от контрактов интеграции. Обычно Contracts используются слоями Application, Infrastructure и Api.

Матрица отражает допустимые ссылки между проектами на уровне решения. Она не означает, что каждая зависимость должна использоваться на практике.


6.10 Dependency Injection
Регистрация зависимостей выполняется исключительно в проекте Order.Api.
Каждый слой предоставляет собственный метод регистрации.

Например:
builder.Services
    .AddApplication()
    .AddInfrastructure(builder.Configuration);

Такой подход инкапсулирует регистрацию компонентов каждого слоя и позволяет сохранить минимальный объём конфигурации в Program.cs.
Domain не содержит регистрации зависимостей.


6.11 Архитектурные ограничения
Для сохранения целостности архитектуры в проекте действуют следующие правила:
Domain никогда не обращается к Infrastructure.
Application никогда не использует DbContext напрямую.
Api никогда не содержит бизнес-логики.
Infrastructure не принимает архитектурных решений — она лишь реализует необходимые интерфейсы.
Между сервисами запрещены прямые ссылки на проекты друг друга.

Соблюдение перечисленных правил рекомендуется контролировать автоматически с помощью Architecture Tests. Это позволяет обнаруживать нарушения архитектурных зависимостей на этапе сборки проекта.

Единственным способом взаимодействия между сервисами являются:
HTTP API;
Integration Events;
Commands через брокер сообщений.

Такой подход обеспечивает слабую связанность (Loose Coupling) и позволяет независимо развивать и развёртывать каждый сервис.


7. Saga и обмен сообщениями
7.1 Назначение
В системе PizzaSaga отсутствуют распределённые транзакции между сервисами.
Каждый микросервис владеет собственной базой данных и самостоятельно отвечает за согласованность своих данных.
Для координации длительных бизнес-процессов используется паттерн Saga, а обмен данными между сервисами осуществляется посредством асинхронных сообщений через RabbitMQ.

Такой подход позволяет:
исключить распределённые транзакции;
обеспечить слабую связанность сервисов;
повысить отказоустойчивость системы;
независимо масштабировать сервисы;
поддерживать длительные бизнес-процессы.


7.2 Общая схема взаимодействия
В качестве брокера сообщений используется RabbitMQ.
Для построения обмена сообщениями применяется библиотека MassTransit.

                HTTP
                  │
                  ▼
           PizzaSaga.ApiGateway
                  │
                  ▼
             Order.Api
                  │
                  ▼
        Order.Application
                  │
                  ▼
          Order State Machine
                  │
        ┌─────────┴─────────┐
        ▼                   ▼
Reserve Inventory     Reserve Payment
        │                   │
        ▼                   ▼
Stock Service        Payment Service
        │                   │
        └─────────┬─────────┘
                  ▼
          Order State Machine
                  │
                  ▼
            Order Completed

Что такое State Machine
State Machine (машина состояний) — это обычный класс, который хранит описание жизненного цикла Saga.
Выглядит примерно так:

Order.Application
│
├── Commands
├── Queries
├── Consumers
├── StateMachines
│      ├── OrderStateMachine.cs
│      └── OrderState.cs
└── ...

Где она находится
Она находится внутри Order Service:

               Order Service
+------------------------------------------+
|                API                       |
|                    │                     |
|                    ▼                     |
|             Application                  |
|                    │                     |
|                    ▼                     |
|         Order State Machine              |
|                    │                     |
|		Saga (MassTransit State Machine)   |
|      ┌─────────────┴─────────────┐       |
|      ▼                           ▼       |
| Publish ReserveInventoryCommand   Publish ReservePaymentCommand
+------------------------------------------+
               │                     │
               ▼                     ▼
        Stock Service        Payment Service

Order Saga является оркестратором бизнес-процесса, а Order Service является хостом этой Saga.
Остальные сервисы выполняют только собственную бизнес-операцию и не знают о существовании друг друга.


7.3 Почему используется Saga
Создание заказа включает несколько независимых операций:
создание заказа;
резервирование товаров;
резервирование оплаты;
подтверждение заказа.

Каждая операция выполняется как локальная транзакция соответствующего сервиса.

Использование распределённой транзакции Two-Phase Commit (2PC, двухфазный коммит) в подобной архитектуре приводит к высокой связанности сервисов, блокировке ресурсов до завершения операции, снижению производительности и усложнению масштабирования.
Поэтому вместо распределённой транзакции используется Saga, которая оперирует локальными транзакциями, обеспечивает согласованность в конечном итоге (eventual consistency) и не блокирует ресурсы глобально.
Saga представляет собой последовательность локальных транзакций, объединённых общим бизнес-процессом.
Каждая локальная транзакция либо подтверждает выполнение операции, либо инициирует компенсирующее действие.


7.4 Оркестратор Saga
В проекте применяется оркестрируемая Saga (Orchestrated Saga).
Оркестрируемая Saga (Orchestrated Saga) — это паттерн управления распределенными транзакциями в микросервисной архитектуре, где специальный центральный сервис (оркестратор) берет на себя координацию всех шагов бизнес-процесса, отправляя команды остальным сервисам и запуская компенсационные действия при сбоях.

Оркестратором является Order Service.
Он управляет последовательностью выполнения операций и принимает решения о переходе между состояниями.
Для реализации используется MassTransit State Machine.

Основные обязанности оркестратора:
создание экземпляра Saga;
управление состояниями заказа;
отправка команд другим сервисам;
обработка интеграционных событий;
запуск компенсирующих операций;
завершение бизнес-процесса.

Остальные сервисы не знают о существовании Saga и не взаимодействуют друг с другом напрямую.

Состояния экземпляра Saga (State Machine).
В процессе выполнения экземпляр Saga последовательно переходит между следующими состояниями:
                Initial
                    │
                    ▼
			AwaitingInventory
          │                 │
          │                 ▼
          │            Cancelled
          ▼
			AwaitingPayment
          │                 │
          │                 ▼
          │            Cancelled
          ▼
      Completed

В виде таблицы:
Состояние						Что означает
Initial							Начальное состояние до создания экземпляра Saga. При получении первого события создаётся новый экземпляр и выполняется переход в следующее состояние.
AwaitingInventoryReservation	Отправлена команда ReserveInventoryCommand, ожидается событие InventoryReservedEvent или InventoryReservationFailedEvent.
AwaitingPaymentReservation		Товар успешно зарезервирован, отправлена команда ReservePaymentCommand, ожидается PaymentReservedEvent или PaymentReservationFailedEvent.
Completed						Все этапы успешно завершены, заказ подтверждён.
Cancelled						Бизнес-процесс завершён с отменой заказа (из-за ошибки или после выполнения компенсации).

Экземпляр Saga сохраняется в отдельной записи базы данных. Помимо текущего состояния, он содержит данные, необходимые для продолжения выполнения бизнес-процесса после получения очередного сообщения.


7.5 Жизненный цикл заказа
Ниже приведён типичный сценарий успешного выполнения заказа.

Create Order (HTTP POST /orders)
        │
        ▼
OrderCreated (Domain Event)
        │
        ▼
ReserveInventoryCommand
        │
        ▼
InventoryReservedEvent
        │
        ▼
ReservePaymentCommand
        │
        ▼
PaymentReservedEvent
        │
        ▼
OrderCompleted (Domain Event)
        │
        ▼
OrderCompletedEvent (Integration Event)

После получения ожидаемых интеграционных событий Saga выполняет переход между состояниями. 
Доменные события используются внутри Order Service для изменения состояния заказа и публикации соответствующих интеграционных событий.
После успешного резервирования товара и оплаты Order Service переводит заказ в состояние Completed, генерирует доменное событие OrderCompleted, которое затем преобразуется в интеграционное событие OrderCompletedEvent.


7.6 Компенсирующие операции
Если одна из бизнес-операций завершается ошибкой, Saga запускает компенсирующие операции, позволяющие вернуть систему в согласованное состояние.
Компенсация представляет собой отдельную бизнес-операцию. Она не является откатом транзакции базы данных. Каждый микросервис самостоятельно отменяет изменения, которые ранее успешно выполнил.
Ниже приведён пример компенсации при ошибке резервирования оплаты.

Create Order
      │
      ▼
OrderCreated (Domain Event)
      │
      ▼
ReserveInventoryCommand
      │
      ▼
InventoryReservedEvent
      │
      ▼
ReservePaymentCommand
      │
      ▼
PaymentReservationFailedEvent
      │
      ▼
ReleaseInventoryCommand
      │
      ▼
InventoryReleasedEvent
      │
      ▼
OrderCancelled (Domain Event)
      │
      ▼
OrderCancelledEvent

Получив событие PaymentReservationFailedEvent, Saga переходит по ветви компенсации и отправляет команду ReleaseInventoryCommand.
После успешного освобождения товара Stock Service публикует событие InventoryReleasedEvent. 
Получив его, Order Service переводит заказ в состояние Cancelled, генерирует доменное событие OrderCancelled, которое затем преобразуется в интеграционное событие OrderCancelledEvent.

Типовые сценарии
В рамках процесса оформления заказа возможны следующие бизнес-сценарии:
1. Успешное выполнение — товар успешно зарезервирован, денежные средства успешно зарезервированы, заказ переводится в состояние Completed.
2. Ошибка резервирования товара — публикуется событие InventoryReservationFailedEvent, Saga переводит заказ в состояние Cancelled, компенсация не требуется, поскольку ни одна из последующих операций ещё не была выполнена.
3. Ошибка резервирования оплаты — после получения события PaymentReservationFailedEvent Saga отправляет команду ReleaseInventoryCommand, после завершения компенсации заказ переводится в состояние Cancelled.

Примечание.
Помимо бизнес-ошибок, Saga обрабатывает инфраструктурные ситуации: временную недоступность сервисов, таймауты, повторную доставку сообщений, повторное выполнение команд и другие сбои распределённой системы. Эти механизмы относятся к обеспечению надёжности обмена сообщениями и не изменяют бизнес-логику жизненного цикла заказа.


7.7 Обмен сообщениями
Взаимодействие между микросервисами осуществляется посредством обмена сообщениями через брокер сообщений.

Используются два основных типа сообщений:
- Commands (команды);
- Integration Events (интеграционные события).

Commands (Команды)
Команда представляет собой запрос на выполнение определённого действия.
Команда направляется конкретному сервису, который является единственным обработчиком данного сообщения (модель взаимодействия 1-к-1).
После обработки команды сервис не возвращает прямой ответ отправителю. Вместо этого он публикует одно из интеграционных событий, сообщающее о результате выполнения операции.

Примеры команд:
- ReserveInventoryCommand
- ReleaseInventoryCommand
- ReservePaymentCommand

Integration Events (Интеграционные события)
Интеграционное событие уведомляет другие микросервисы о произошедшем факте.
События публикуются по модели 1-ко-многим. Отправитель не знает и не должен знать, какие сервисы подпишутся на опубликованное событие.
Интеграционные события не предполагают получения ответа.

Примеры интеграционных событий:
- InventoryReservedEvent — товар успешно зарезервирован;
- InventoryReservationFailedEvent — резервирование товара завершилось ошибкой;
- InventoryReleasedEvent — ранее зарезервированный товар освобождён;
- PaymentReservedEvent — денежные средства успешно зарезервированы;
- PaymentReservationFailedEvent — резервирование денежных средств завершилось ошибкой;
- OrderCompletedEvent — заказ успешно завершён;
- OrderCancelledEvent — заказ отменён.

Одно интеграционное событие может одновременно обрабатываться несколькими микросервисами независимо друг от друга.


7.8 Domain Events и Integration Events
В системе используются два уровня событий: Domain Events и Integration Events.
Такое разделение позволяет отделить внутреннюю модель предметной области от взаимодействия между микросервисами.

Domain Events
Domain Event описывает изменение состояния предметной области внутри одного микросервиса.
Доменные события используются исключительно внутри сервиса и не публикуются во внешнюю систему.

Примеры доменных событий в Order Service:
OrderCreated
OrderCompleted
OrderCancelled

Доменные события позволяют реализовать внутреннюю бизнес-логику, не связывая её с механизмами обмена сообщениями между сервисами.

Integration Events
Integration Event предназначен для обмена информацией между микросервисами.
После обработки доменного события сервис при необходимости формирует и публикует соответствующее интеграционное событие.
Все интеграционные события являются частью проекта PizzaSaga.Contracts, который содержит общий контракт взаимодействия между сервисами.

Примеры:
InventoryReservedEvent
InventoryReservationFailedEvent
InventoryReleasedEvent
PaymentReservedEvent
PaymentReservationFailedEvent
OrderCompletedEvent
OrderCancelledEvent

Например, после перевода заказа в состояние Completed Order Service генерирует доменное событие OrderCompleted. Затем оно преобразуется в интеграционное событие OrderCompletedEvent, которое публикуется в брокер сообщений и становится доступным другим микросервисам.
Аналогично, после перевода заказа в состояние Cancelled создаётся доменное событие OrderCancelled, которое преобразуется в OrderCancelledEvent.
Такое разделение позволяет не раскрывать внутреннюю модель предметной области другим сервисам, а также упрощает развитие системы и снижает связанность между её компонентами.


7.9 Надёжная доставка сообщений (Outbox Pattern)
Для обеспечения согласованности между базой данных и брокером сообщений используется Transactional Outbox Pattern.
Во время обработки команды выполняется одна локальная транзакция.

В рамках этой транзакции:
сохраняются изменения бизнес-сущностей;
сохраняется запись в таблице Outbox.

После успешной фиксации транзакции специальный процесс доставки сообщений считывает записи из таблицы Outbox и публикует их в RabbitMQ.

Business Operation
        │
        ▼
Database Transaction
        │
        ├─────────────► Update Aggregate
        │
        └─────────────► Insert Outbox Message
                          │
                          ▼
                 Background Publisher
                          │
                          ▼
                     RabbitMQ

Такой подход предотвращает потерю сообщения между сохранением данных и их публикацией в брокер сообщений.


7.10 Идемпотентность обработчиков
RabbitMQ и MassTransit поддерживают повторную доставку сообщений.
Поэтому каждый обработчик должен быть идемпотентным.
Повторная обработка одного и того же сообщения не должна изменять результат выполнения операции.

Для обеспечения идемпотентности могут использоваться:
уникальный MessageId;
таблица обработанных сообщений;
проверка текущего состояния агрегата.

Выбор конкретного механизма зависит от сценария обработки.


7.11 CorrelationId и Saga
Каждый экземпляр Saga идентифицируется с помощью CorrelationId.
В Saga CorrelationId используется для поиска экземпляра State Machine, которому предназначено поступившее сообщение.

Этот идентификатор:
передаётся между сервисами;
сохраняется в состоянии Saga;
используется при публикации сообщений;
включается в журналы и трассировки.

Благодаря этому можно проследить выполнение всего бизнес-процесса от HTTP-запроса до завершения Saga.


7.12 Обработка ошибок
При возникновении ошибки применяется следующий порядок действий:
локальная транзакция откатывается;
сообщение не публикуется (если транзакция не завершилась);
при необходимости MassTransit выполняет повторную доставку сообщения;
если попытки исчерпаны, сообщение помещается в очередь ошибок;
После исчерпания попыток обработки Saga либо инициирует компенсирующую операцию, либо переводит бизнес-процесс в состояние Cancelled в соответствии с бизнес-логикой.

Такой механизм обеспечивает устойчивость системы к временным сбоям.


7.13 Принципы обмена сообщениями
При разработке сервисов соблюдаются следующие правила:
• сервисы не имеют прямых ссылок друг на друга;
• взаимодействие осуществляется только через PizzaSaga.Contracts;
• сообщения являются неизменяемыми;
• команды описывают намерение выполнить действие;
• Integration Events описывают уже произошедший факт;
• каждый сервис отвечает только за собственные данные;
• обработчики сообщений должны быть идемпотентными;
• Saga координирует выполнение бизнес-процесса, но не реализует бизнес-логику других сервисов.


7.14 Итог
Для координации распределённого бизнес-процесса в PizzaSaga используется оркестрируемая Saga на базе MassTransit State Machine.
Состояние выполнения бизнес-процесса хранится в экземпляре Saga, что позволяет безопасно продолжать выполнение после получения очередного сообщения.
Обмен сообщениями осуществляется через RabbitMQ с использованием команд и интеграционных событий.
Надёжная доставка сообщений обеспечивается Transactional Outbox Pattern, а корректность повторной обработки достигается за счёт идемпотентности обработчиков.
Такое сочетание паттернов позволяет реализовать согласованный бизнес-процесс без использования распределённых транзакций, сохраняя слабую связанность сервисов и высокую отказоустойчивость системы.


8. Observability (наблюдаемость)
Observability в распределённой микросервисной архитектуре — это способность системы предоставлять достаточный объём данных для понимания её внутреннего состояния на основе внешних сигналов. В отличие от классического мониторинга, который отвечает на вопрос «что сломалось?», observability отвечает на более широкий вопрос: «почему и где именно это произошло?».

В контексте данного решения observability является обязательным архитектурным свойством, так как система построена на микросервисах, асинхронном взаимодействии и Saga-оркестрации, где цепочки вызовов могут быть распределены между несколькими сервисами и инфраструктурными компонентами.


8.1 Три столпа observability
Система опирается на три базовых сигнала:

8.1.1. Логи (Logs)
Логи фиксируют дискретные события, происходящие внутри сервисов.

Используются для:
диагностики ошибок
анализа бизнес-сценариев
аудита действий пользователя и системы

Требования:
структурированные логи (JSON)
обязательные поля:
Timestamp
Level
ServiceName
CorrelationId
TraceId / SpanId
отсутствие чувствительных данных (PII, токены)

8.1.2. Метрики (Metrics)
Метрики отражают агрегированное состояние системы во времени.

Используются для:
мониторинга здоровья сервисов
алертинга
анализа нагрузки

Типовые метрики:
latency (p50/p95/p99)
request count
error rate
queue lag (для RabbitMQ)
CPU / memory / GC metrics

Рекомендуемые подходы:
Prometheus-совместимый формат
использование RED/USE методологий:
Rate
Errors
Duration

8.1.3. Трейсы (Distributed Tracing)
Трейс представляет собой полную цепочку выполнения запроса через все сервисы.

Используется для:
анализа распределённых запросов
диагностики задержек в Saga
выявления узких мест

Структура:
Trace → глобальный идентификатор запроса
Span → отдельная операция внутри сервиса

Пример:
API Gateway
 ├── Order.Service (Span 1)
 │     ├── Payment.Service (Span 2)
 │     └── Stock.Service (Span 3)
 
 
8.2 Корреляция запросов
Каждый входящий запрос должен содержать или получать:
CorrelationId — бизнес-идентификатор запроса
TraceId — технический идентификатор распределённого трейсинга

Правила:
CorrelationId передаётся через все сервисы без изменений
TraceId создаётся автоматически системой трейсинга
оба значения логируются в каждом сервисе

Это позволяет:
связывать логи разных сервисов
восстанавливать полный путь запроса
анализировать Saga-цепочки


8.3 Наблюдаемость в Saga
Saga является ключевым источником сложности в observability.

Требования:
каждая Saga-операция должна быть представлена как набор span-ов
каждая компенсационная операция также должна трассироваться
события Saga должны логироваться как бизнес-события

Рекомендуется:
- выделять SagaId
- логировать:
-- старт Saga
-- каждую шаг-операцию
-- компенсации
-- финальный статус (Success / Compensated / Failed)


8.4 Логирование
Рекомендуемый стек:
Serilog (или аналогичный structured logger)
sink в:
Elasticsearch / OpenSearch
Loki (альтернатива)

Стандарты:
логирование только структурированных объектов
запрет строковых «free-text» логов без структуры
единый формат логов во всех сервисах

Пример лог-события:
{
  "Timestamp": "2026-07-06T12:00:00Z",
  "Level": "Information",
  "ServiceName": "Order.Service",
  "CorrelationId": "abc-123",
  "TraceId": "trace-456",
  "Message": "Order created",
  "OrderId": 789
}


8.5 Метрики и мониторинг
Рекомендуемый стек:
Prometheus (сбор метрик)
Grafana (визуализация)
Alertmanager (алерты)

Обязательные дашборды:
- API Gateway: RPS, latency, error rate
- Каждый микросервис: health, dependency failures
Message broker: queue depth, consumer lag
Saga monitoring: success/failure rate, compensation rate


8.6 Трейсинг
Рекомендуемый стек:
OpenTelemetry
Jaeger / Tempo (хранилище и UI)

Требования:
автоматическая инструментализация HTTP
автоматическая инструментализация gRPC (если используется)
ручная инструментализация бизнес-операций

Критично:
все внешние вызовы (HTTP, DB, MQ) должны быть в трейсе
не допускается «разрывов» цепочки


8.7 Контекст и propagation
Контекст запроса должен передаваться через:
HTTP headers
message broker headers (RabbitMQ/Kafka)
gRPC metadata

Передаваемые поля:
CorrelationId
TraceId
SpanId (при необходимости)
UserId (если применимо)


8.8 Алёртинг
Принципы:
алерты должны быть action-driven (требуют действия)
минимизация noise
приоритет SLO-based алертов

Примеры алертов:
error rate > threshold
latency p95 > threshold
queue backlog растёт > N минут
Saga failure rate > baseline


8.9 Non-functional требования
Observability считается обязательным NFR:
100% сервисов должны быть покрыты логами
100% входящих запросов должны быть трассируемы
отсутствие correlation propagation считается критическим дефектом
задержка доставки метрик < 10–30 секунд


8.10 Итог
Observability в системе — это не дополнительный слой, а фундаментальный архитектурный компонент, обеспечивающий:
управляемость микросервисов
прозрачность Saga-оркестрации
ускоренную диагностику инцидентов
контроль качества распределённой системы

Без полноценной observability распределённая система фактически становится «чёрным ящиком», что недопустимо для production-уровня архитектуры.


9. Infrastructure
Назначение
Инфраструктура проекта обеспечивает выполнение всех нефункциональных требований системы:
запуск сервисов;
сетевое взаимодействие;
хранение данных;
обмен сообщениями;
мониторинг;
трассировку;
централизованное логирование;
локальную разработку.

Архитектура инфраструктуры построена таким образом, чтобы максимально соответствовать production-подходам, оставаясь при этом достаточно простой для локальной разработки и сопровождения.

Инфраструктурные компоненты
В состав проекта входят следующие инфраструктурные сервисы.

Компонент			Назначение
API Gateway			Единая точка входа в систему
PostgreSQL			Хранение данных каждого сервиса
RabbitMQ			Асинхронный обмен сообщениями
Seq					Централизованное хранение логов
Prometheus			Сбор метрик
Grafana				Визуализация метрик
Jaeger				Distributed Tracing
Aspire Dashboard	Локальная диагностика сервисов

Контейнеризация
Все сервисы и инфраструктурные компоненты запускаются в отдельных Docker-контейнерах.

Каждый микросервис является полностью независимой единицей развертывания и содержит:
собственное приложение;
собственную базу данных;
собственную конфигурацию;
собственные миграции.

Такой подход обеспечивает независимое развитие сервисов и соответствует принципам микросервисной архитектуры.

Управление инфраструктурой
Для локальной разработки инфраструктура управляется .NET Aspire.
AppHost описывает состав распределённого приложения и автоматически запускает необходимые сервисы и инфраструктурные компоненты.

В проекте используются:
Docker Desktop;
.NET Aspire.

Docker Desktop предоставляет среду выполнения контейнеров, а .NET Aspire отвечает за:
запуск распределённого приложения;
создание и подключение инфраструктурных ресурсов;
управление зависимостями между сервисами;
передачу конфигурации через переменные окружения;
мониторинг состояния сервисов;
просмотр логов через Aspire Dashboard.

Такой подход позволяет запускать всю систему одной командой без необходимости вручную управлять контейнерами или использовать Docker Compose.

Базы данных
Каждый микросервис владеет собственной базой данных.
Общий доступ к данным между сервисами запрещён.

Order Service
        │
        ▼
Order Database

Payment Service
        │
        ▼
Payment Database

Kitchen Service
        │
        ▼
Kitchen Database

Все взаимодействие между сервисами осуществляется исключительно через: события RabbitMQ.
Подобная изоляция уменьшает связанность сервисов и позволяет независимо изменять схемы данных каждого сервиса.

Конфигурация
Конфигурация сервисов хранится отдельно от приложения.

Используются стандартные механизмы ASP.NET Core:
appsettings.json;
appsettings.Development.json;
переменные окружения;
User Secrets (для локальной разработки).

Секреты не хранятся в репозитории.

Управление миграциями
Каждый сервис самостоятельно управляет своей схемой базы данных.
Миграции выполняются при запуске сервиса либо через отдельный процесс развёртывания.
Это позволяет обновлять базы данных независимо друг от друга без влияния на остальные сервисы.

Сетевая модель
Все сервисы находятся в общей внутренней сети Docker.

Внешний доступ предоставляется только следующим компонентам:
API Gateway;
Aspire Dashboard (локальная разработка);
Grafana;
Seq;
Jaeger.

Внутренние сервисы не публикуют свои порты наружу без необходимости.

Отказоустойчивость
Система проектируется с учётом возможности временной недоступности отдельных компонентов инфраструктуры.

Для этого используются:
повторные попытки (Retry);
таймауты;
асинхронное взаимодействие через RabbitMQ;
идемпотентная обработка сообщений;
Health Checks.

Такой подход позволяет локализовать сбои и снизить вероятность каскадных отказов.

Масштабирование
Архитектура допускает независимое масштабирование отдельных сервисов.

При увеличении нагрузки возможно горизонтальное масштабирование:
API Gateway;
Order Service;
Payment Service;
Kitchen Service;
Notification Service.

При этом остальные сервисы продолжают работать без изменений.

Production-подход
Проект ориентирован на демонстрацию production-ready архитектуры.

Поэтому используются следующие практики:
контейнеризация всех сервисов;
независимые базы данных;
обмен сообщениями через RabbitMQ;
централизованная наблюдаемость;
изолированные конфигурации;
независимое развертывание сервисов;
автоматизация локального окружения через .NET Aspire.

Такой подход обеспечивает хорошую масштабируемость, низкую связанность компонентов и упрощает дальнейшее развитие системы.


10. Testing Strategy
Цель
Стратегия тестирования предназначена для обеспечения качества системы на всех этапах разработки. В проекте используется многоуровневый подход к тестированию, при котором каждый уровень решает собственную задачу и не дублирует остальные.

Основные цели:
проверка корректности бизнес-логики;
проверка взаимодействия компонентов системы;
проверка интеграции с внешней инфраструктурой;
проверка полного пользовательского сценария;
предотвращение нарушения архитектурных ограничений.
Пирамида тестирования

В проекте используется классическая пирамида тестирования.

                System Tests
            -------------------
           Integration Tests
        -------------------------
             Unit Tests

Каждый уровень тестирования отвечает за собственную область ответственности.

Unit Tests
Назначение
Unit-тесты проверяют отдельные элементы бизнес-логики в полной изоляции от инфраструктуры.
Такие тесты должны выполняться максимально быстро и не требовать подключения к базе данных, брокеру сообщений или другим внешним сервисам.

Что тестируется
В первую очередь покрываются тестами:
Domain Entities;
Value Objects;
Domain Services;
бизнес-правила;
политики (Policy);
валидация доменной модели.

В проекте будет большое количество Unit-тестов, покрывающих бизнес-логику доменной модели и отдельных компонентов приложения.
Например:
Создание заказа с корректным набором позиций.
Запрет создания заказа без позиций.
Расчёт итоговой стоимости заказа с учётом количества товаров.
Корректное изменение статуса заказа в соответствии с бизнес-правилами.
Валидация команд и Value Objects.

Что не тестируется
Unit-тестами не покрываются:
контроллеры;
DTO;
конфигурация DI;
EF Core;
MassTransit;
RabbitMQ;
PostgreSQL;
сериализация;
простые классы без собственной логики.

Integration Tests
Назначение
Интеграционные тесты проверяют корректность взаимодействия приложения с инфраструктурными компонентами.
В отличие от Unit-тестов, здесь используется реальная инфраструктура.

Что тестируется
Интеграционные тесты покрывают:
PostgreSQL;
Entity Framework Core;
миграции базы данных;
репозитории;
транзакции;
RabbitMQ;
MassTransit;
публикацию и получение сообщений;
Outbox (при использовании);
взаимодействие с внешними сервисами через тестовые реализации или локальные контейнеры.

Например:
Сохранение и чтение заказа из PostgreSQL через Entity Framework Core.
Применение миграций базы данных при запуске приложения.
Публикация и получение сообщений через RabbitMQ и MassTransit.
Сохранение и публикация сообщений через Outbox.
Корректная работа HTTP-клиента при взаимодействии с внешним сервисом.

Основная задача — убедиться, что приложение корректно работает с реальной инфраструктурой.

System Tests
Назначение
System-тесты проверяют работу всей системы как единого целого.

Во время выполнения запускаются все необходимые сервисы проекта, после чего тест воспроизводит полный пользовательский сценарий.
Например:

POST /orders
        │
        ▼
Order Service
        │
        ▼
Reserve Inventory
        │
        ▼
Stock Service
        │
        ▼
Reserve Payment
        │
        ▼
Payment Service
        │
        ▼
Complete Order

Количество System Tests намеренно остаётся небольшим. Они покрывают наиболее важные пользовательские сценарии и проверяют работу распределённой системы целиком.
Рекомендуемый минимальный набор включает следующие сценарии:
1. Создание заказа → успешное завершение Saga. 
2. Недостаточно товара → компенсация и отмена заказа. 
3. Ошибка оплаты → освобождение резерва на складе. 
4. Повторная доставка сообщения → отсутствие повторной обработки. 
5. Проверка публикации событий через Outbox. 

System-тест подтверждает, что:
HTTP API работает корректно;
Saga завершается успешно;
сообщения проходят через RabbitMQ;
сервисы взаимодействуют между собой;
итоговое состояние заказа соответствует ожидаемому.

Подобные тесты наиболее близки к поведению системы в рабочей среде.

Между тремя типами тестов получается чёткое разделение ответственности:
Unit Tests отвечают за корректность бизнес-логики.
Integration Tests подтверждают корректную работу инфраструктуры (БД, брокер сообщений, HTTP-клиенты и т.п.).
System Tests проверяют сквозные бизнес-сценарии и взаимодействие сервисов как единой распределённой системы.

Architecture Tests
Назначение
Architecture Tests автоматически контролируют соблюдение архитектурных ограничений проекта.
Такие тесты не проверяют бизнес-логику, а защищают архитектуру от постепенной деградации.
Они позволяют гарантировать соблюдение принятых архитектурных решений независимо от количества разработчиков и времени существования проекта.

Проверяемые правила
В проекте рекомендуется контролировать следующие ограничения:
Domain не зависит от Infrastructure;
Domain не зависит от API;
Application не зависит от API;
Application не зависит от Infrastructure;
Entity Framework Core используется только в Infrastructure;
Controllers располагаются только в проекте API;
Consumers располагаются только в слое API (или выделенном Messaging-проекте);
Integration Events находятся только в проекте Contracts;
публичные HTTP API используют DTO, а не Domain Entity.

Подобные проверки выполняются автоматически при запуске тестов и позволяют обнаружить нарушение архитектуры ещё до проведения Code Review.

Инструменты
Для тестирования используются следующие инструменты:

Инструмент				Назначение
xUnit					Фреймворк модульного тестирования
FluentAssertions		Удобная проверка результатов тестов
Testcontainers			Запуск PostgreSQL и RabbitMQ в Docker-контейнерах
ASP.NET Core Test Host	Интеграционное тестирование HTTP API
.NET Aspire Testing		Запуск распределённой системы для System Tests
NetArchTest.Rules		Проверка архитектурных ограничений

Организация тестовых проектов
Тестовые проекты разделяются на два уровня:
локальные тесты сервиса, расположенные рядом с исходным кодом сервиса;
общесистемные тесты, расположенные в отдельной директории tests в корне решения.

Локальные тесты являются частью конкретного сервиса и развиваются вместе с ним.
Например, для сервиса Order структура выглядит следующим образом:

Services
└── Order
    │
    ├── Order.Api
    ├── Order.Application
    ├── Order.Domain
    ├── Order.Infrastructure
    │
    ├── Order.ArchitectureTests
    ├── Order.UnitTests
    └── Order.IntegrationTests

Такое размещение позволяет хранить тесты рядом с тестируемым сервисом и упрощает сопровождение при изменении его функциональности.
Общесистемные тесты располагаются отдельно, поскольку проверяют взаимодействие нескольких сервисов и работу системы в целом.

tests
│
└── PizzaSaga.SystemTests

PizzaSaga.SystemTests не относится к какому-либо одному сервису. Он запускает распределённую систему через .NET Aspire и проверяет сквозные пользовательские сценарии, включая взаимодействие сервисов по HTTP и через RabbitMQ, выполнение Saga и корректность итогового состояния системы.

Такая организация позволяет чётко разделить ответственность между тестами отдельного сервиса и тестами, проверяющими работу всей распределённой системы.

Запуск в CI/CD
При выполнении конвейера непрерывной интеграции рекомендуется следующая последовательность:
Сборка решения.
Выполнение Architecture Tests.
Выполнение Unit Tests.
Выполнение Integration Tests.
Выполнение System Tests.
Публикация результатов тестирования.

Такой порядок позволяет максимально быстро обнаруживать ошибки. Наиболее быстрые проверки выполняются первыми, а наиболее дорогостоящие — после успешного завершения предыдущих этапов.

Принципы тестирования
При разработке проекта рекомендуется придерживаться следующих принципов:
каждый тест проверяет только одно поведение;
тесты должны быть независимыми друг от друга;
Unit-тесты не используют внешнюю инфраструктуру;
интеграционные тесты используют реальные инфраструктурные компоненты;
архитектурные ограничения проверяются автоматически;
критические пользовательские сценарии покрываются System-тестами.

Такой подход обеспечивает баланс между скоростью выполнения тестов, стоимостью их сопровождения и уровнем доверия к качеству системы.


